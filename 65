int rob(int[] nums) {
    int prev1 = 0, prev2 = 0;

    for (int n : nums) {
        int pick = prev2 + n;
        int notPick = prev1;
        int curr = Math.max(pick, notPick);

        prev2 = prev1;
        prev1 = curr;
    }
    return prev1;
}
